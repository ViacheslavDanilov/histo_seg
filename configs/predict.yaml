defaults:
- main
- _self_

data_dir: data/demo/input
save_dir: data/demo/output
model_dir: models/DeepLabV3/fold_1  # TODO: new variable
output_size: [1000, 1000] # TODO: think of it
device: cuda   # cpu or cuda

# TODO: check these parameters
model_name: DeepLabV3_regnetx_64    # TODO: I think we can remove it
model_weights: models/DeepLabV3/fold_1/weights.ckpt # TODO: I think we can remove it
classes:  # TODO: I think we can remove it
- Arteriole lumen
- Arteriole media
- Arteriole adventitia
- Capillary lumen
- Capillary wall
- Venule lumen
- Venule wall
- Immune cells
- Nerve trunks
architecture: DeepLabV3 # TODO: I think we can remove it
encoder: timm-regnetx_064 # TODO: I think we can remove it
input_size: 896 # TODO: I think we can remove it
batch_size: 2 # TODO: I think we can remove it
